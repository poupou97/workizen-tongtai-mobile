# ADR Index — quyết định kiến trúc đang hiệu lực

| ADR | Quyết định | Trạng thái | Ngày |
|---|---|---|---|
| [ADR-TON-001](ADR-TON-001-app-separation-single-app-flavors.md) | App separation: tách rõ Platform/Product layer; không phụ thuộc code feature Hub; module phải extractable. Phần "single-app + flavors" bị **supersede bởi ADR-TON-003**; ba yêu cầu kiến trúc giữ nguyên hiệu lực | ✅ ACCEPTED (Founder) | 2026-07-16 |
| [ADR-TON-002](ADR-TON-002-di-riverpod.md) | DI/state = **Riverpod duy nhất** (GetIt trong AC cũ = lỗi spec) | ✅ ACCEPTED (Founder) | 2026-07-16 |
| [ADR-TON-003](ADR-TON-003-repo-split.md) | Repo độc lập `workizen-tongtai-mobile` (supersede phần flavors của ADR-TON-001); Hub = upstream fetch-only, không thêm build flavor | ✅ ACCEPTED (Founder) | 2026-07-22 |
| [ADR-TON-004](ADR-TON-004-chat-persistence-local-only.md) | Chat persistence **local-only** (bảng v4, KHÔNG vào sync outbox — D-5 đã xác nhận: Phase 2 không backend/sync); mã hoá = chuẩn nền tảng, SQLCipher là option chờ Founder | ✅ ACCEPTED (Founder) | 2026-07-23 |
| [ADR-TON-005](ADR-TON-005-analytics-firebase-operational.md) | Analytics (D-7 Updated): Phase 2 cho phép **Firebase Analytics + Crashlytics** (operational-only, giám sát closed beta); CẤM ad/marketing/profiling — red line giữ nguyên | ✅ ACCEPTED (Founder) | 2026-07-23 |
| [ADR-TON-006](ADR-TON-006-workizen-ai-router.md) | AI Provider (D-9 Updated): **Workizen AI Router** đa provider (Gemini/xAI/Claude/OpenRouter/Cerebras/Ollama), 3 chế độ Managed/BYOK/Local — supersede xAI-first; Phase 2 = BYOK + Local | ✅ ACCEPTED (Founder) | 2026-07-23 |
| [ADR-TON-007](ADR-TON-007-localization-appstrings.md) | Localization = **custom `AppStrings` + `LanguageNotifier`** (mirror Hub, KHÔNG ARB/gen-l10n/gói ngoài); `context.l10n`, đổi ngôn ngữ runtime EN+VI; migrate dần Boy-Scout (WTM-119) | ✅ ACCEPTED (Founder-directed) | 2026-07-24 |
| [ADR-TON-008](ADR-TON-008-drift-persistence-user-data-first.md) | Drift persistence: **UI→Controller→Repository→Store→Drift**, Repository quyết định nguồn; **User Data First** (DB thật KHÔNG seed sample, user mới rỗng; sample chỉ Demo Mode); **Local Business** root aggregate (mở rộng multi-business/cloud/login sau). Finance-first (WTM-120) | ✅ ACCEPTED (Founder-directed) | 2026-07-24 |
| [ADR-TON-009](ADR-TON-009-persistence-snapshot-then-normalize.md) | Persistence mapping: **structured columns + versioned domain snapshot** (option B, transitional) khi domain lệch schema — lossless, additive migration; promote field JSON→column/child-table theo **business trigger** (không normalize sớm, không JSON mãi). Inventory-first (WTM-121); TODO WTM-122; inventory `PERSISTENCE-INVENTORY.md`. **Amendment 2 (WTM-333, 2026-08-12):** với **`Product`**, cơ chế mở rộng **chính** chuyển sang **tầng thuộc tính động** (WTM-334) — `domainSnapshot` chỉ còn giữ tương thích ngược + `imagePaths` (tới khi có `Media`); **cấm thêm extension field mới vào snapshot**; trường mới load-bearing ⇒ promote thành cột, spec/metadata ⇒ attribute. Snapshot pattern (option B) **không** bị supersede cho các domain khác. Bảng phân loại: `docs/05-DATA/COMMERCE-ATTRIBUTE-MODEL.md` | ✅ ACCEPTED (Founder-directed) | 2026-07-24 |
| [ADR-TON-012](ADR-TON-012-business-context-aggregate-root.md) | **BusinessContext = Aggregate Root** (Progressive Aggregation: Phase 1 = metrics+customers+orders+inventory; sau thêm opportunity/journey/timeline/goals/finance). **AI chỉ đọc BusinessContext**, tuyệt đối không repo/Drift. Chain Repos→BusinessMetricsService→BusinessContext→BusinessHealth→Home→AI. **Opportunity tách 2 phase** (P1 rule-based High Value/Risk/Urgent/Stale — KHÔNG khóa AI; P2 AI Win Prob/Recommendation/Summary). First: WTM-129 | ✅ ACCEPTED (Founder) | 2026-07-25 |
| [ADR-TON-011](ADR-TON-011-business-metrics-service-kpi-ssot.md) | **BusinessMetricsService = SoT cho KPI** (Repositories→BusinessMetricsService→Reports→Home→BusinessContext→AI). KPI đầu: revenue·orders·customers·AOV (billable). Reports/Home **tái dùng**, KHÔNG tự tính; KPI tương lai (top products, repeat, monthly, CLV, forecast) **mở rộng service**; input tương lai cho BusinessContext+AI (consume aggregate, không chạm repo/Drift). First: Reports (WTM-127); next Home (WTM-128) | ✅ ACCEPTED (Founder) | 2026-07-25 |
| [ADR-TON-010](ADR-TON-010-orders-independent-capability-and-layered-dod.md) | **Orders = capability độc lập** (sở hữu lifecycle/revenue/items/payment/invoice/shipment/returns; Consumer chỉ launch "Create Order", KHÔNG chứa order logic; Reports/Home consume OrderRepository) — G-2. **Home = User Data First** + zero-state CTA (G-1). **AI deferred** đến khi xong Data Foundation theo sequence Consumer→Orders→Reports→Home KPI→Opportunity→Journey→Timeline→BusinessContext→AI (G-3). **Layered DoD**: capability DONE chỉ khi đủ UI→Repository→Persistence→Report→BusinessContext→AI-Ready + không downstream nào còn xài Sample. Orders-first (WTM-125) | ✅ ACCEPTED (Founder) | 2026-07-25 |
| [ADR-TON-018](ADR-TON-018-backup-format-v2-and-restore.md) | **`.ttbk` v2 + Replace restore** — v1 là **một CSV mã hoá của một dataset** (3/6 repo, **mất `order.id` + `OrderItem.productId`** ⇒ đứt Inventory↔Orders, enum lưu bằng **nhãn tiếng Việt**, version hoá container chứ không phải schema) ⇒ **không khôi phục được doanh nghiệp**. v2 = snapshot **toàn miền, lossless, có version**: manifest plaintext (`formatVersion`·`contentSchemaVersion`·`appVersion`·`databaseSchemaVersion`·`backupId`·`createdAt`·`encryption`·`compression`·SHA-256+độ dài), **record counts nằm TRONG payload** (file mã hoá không rò rỉ quy mô kinh doanh), enum bằng **mã canonical**, mã enum lạ = **bản ghi hỏng chứ không phải default**. **SHA-256 = chống hỏng, KHÔNG phải chống giả mạo** (chỉ AES-GCM mới xác thực). **Restore = Replace only** (Founder; Merge là capability riêng, cấm nhét lén): validate toàn bộ read-only → preview → xác nhận phá huỷ → **tạo + verify** bản sao lưu an toàn → **một transaction** {xoá theo FK → ghi theo phụ thuộc → verify counts+FK} → commit → invalidate. Không tạo/verify được bản an toàn ⇒ **không xoá gì**. Version mới hơn/hỏng ⇒ **chặn**, không partial import. CSV export giữ nguyên cho Excel. **Amendment 1 (WTM-165, Founder Note):** `.ttbk` là **Business Snapshot Package** — backup chỉ là **một** capability; manifest chừa `packageKind`·`datasets`·`redaction` cho Restore·Clone·Migration·Demo·AI Sandbox·Support Bundle·Analytics Exchange. **Mã lạ ⇒ `unknown`, không bao giờ ⇒ `backup`**; vắng mặt ⇒ mặc định (file v2 cũ vẫn restore được); khoá lạ ⇒ bỏ qua. Lệnh cấm nằm ở **restore contract**, không ở format — gói bộ phận/đã lược bỏ vẫn là gói **hợp lệ**, chỉ không restore được. First: WTM-164 · WTM-165 | ✅ ACCEPTED (Founder) | 2026-07-31 |
| [ADR-TON-017](ADR-TON-017-error-handling-seam.md) | **Shared Error-Handling Seam** — `ScreenState<T>` sáu trạng thái (loading·ready·empty·insufficient·refreshing·failed; `empty`/`insufficient` là CÂU TRẢ LỜI, `failed` là KHÔNG có câu trả lời) + `TongtaiFailure{kind,code,detail}` phân loại (storage·network·permission·configuration·unexpected; permission/configuration KHÔNG mời retry) + `ScreenDataController` (refresh lỗi **giữ dữ liệu cũ** ⇒ stale, drop response lạc thế hệ) + `runTongtaiAction` (ghi không được im lặng) + `TongtaiScreenData`/`TongtaiAsyncScreenData` (một cách render, stable key `<prefix>-loading|empty|insufficient|error|error-retry|stale`). **Loading không animation** (local-first, và để pumpAndSettle không treo). **Riêng tư:** `detail` chỉ trên máy người dùng; telemetry `screen_error` chỉ `kind`+`code`+`screen`; `toString()` bỏ `detail` vì crash reporter ghi nó. **Cấm trong `ui/`:** catch thủ công · spinner tự chế · FutureBuilder/`.when` · nhiều `tongtaiDatabaseProvider`. First: WTM-148 | ✅ ACCEPTED (Founder) | 2026-07-31 |
| [ADR-TON-016](ADR-TON-016-capability-context-and-predictive-foundation.md) | **Capability Context + Rule Twin + AI Runtime Boundary** — BusinessContext CHỈ giữ identity/metadata/health/lightweight summary/snapshot (**cấm God Object**); phân tích chuyên sâu tách thành Capability Context độc lập tải on-demand (Revenue · Customer · … ). One Data Path mở rộng: `Repository → Aggregation Services → Capability Context → BusinessContext (summary) → Rule Twin → AI Router → AI → (Tool Runtime optional) → Human`; cấm duplicate aggregation/summary/counting/query. **Rule Twin authoritative** (chạy không cần AI/mạng/key), trả `result+confidence+sufficiency+reasonCodes+version`, `result==null` ⟺ insufficient — cấm kết luận giả. **AI chỉ giải thích**, không sửa forecast/risk, không query DB, không mutate/execute. Tool Runtime thiết kế sẵn nhưng **KHÔNG** triển khai (không tool-calling, không ReAct). First: WTM-149 | ✅ ACCEPTED (Founder) | 2026-07-30 |
| [ADR-TON-015](ADR-TON-015-ui-maturity-and-one-data-path.md) | **UI Implementation Maturity Model (L0–L4)** + **One Data Path** (`Repository → Context Provider → BusinessContext → Screen`; cấm parallel demo state/cache, hardcode business data, mỗi màn tự tính summary) + **Cross-screen Contract** `Summary Count == Domain Visible Records` + **Stable Test IDs** bắt buộc cho L2+. Level trong Jira PHẢI == level thật trong code; cấm đóng story ở level cao hơn thực tế. First: WTM-147 | ✅ ACCEPTED (Founder) | 2026-07-30 |
| [ADR-TON-014](ADR-TON-014-sample-data-seeding.md) | **Sample seeds vào production repos** (P0 Regression Audit §1): 1 nguồn cho MỌI màn + AI; "Xem thử Demo" = seed id `sample-` (idempotent, link remap); sample = row thường (sửa/xóa được); "Xóa dữ liệu mẫu" chỉ xóa prefix; bỏ `TongtaiHomeScreen.demo` + mọi kSample fallback trong lib. Supersede demo-song-song của WTM-128. First: WTM-144 | ✅ ACCEPTED (Founder) | 2026-07-30 |
| [ADR-TON-013](ADR-TON-013-staged-ai-activation.md) | **Staged AI activation (G-3A→G-3D)** — mở G-3 theo bậc pre-approved, auto-progress, TẤT CẢ read-only: G-3A AI Summary (chỉ đọc BusinessContext) → G-3B Recommendation (không mutate/execute) → G-3C Planner (không auto-execute) → G-3D BusinessHealth AI (rule-based vẫn là fallback mặc định). Defaults khóa kèm: Opportunity `Rule Engine → AI Scoring → Ranking → Explanation` (KHÔNG thay rule engine); Journey progress **auto-derived** từ data; **WTM-122 CLOSED**. Invariants: AI chỉ thấy `businessContextPromptText` (test phải chứng minh); mọi AI feature có twin rule-based; business rỗng không tốn provider call. First: WTM-116 | ✅ ACCEPTED (Founder) | 2026-07-29 |
| [ADR-TON-019](ADR-TON-019-capability-context-performance.md) | **Capability Context Performance** — WTM-166 đo được: một cold start đọc `orders` **×5**, `customers`/`goals` **×4**, và **mỗi lần đọc đều chính đáng** (hệ quả trực tiếp của ADR-TON-016: Capability Context **độc lập, tải on-demand**). Không phải bài toán cold-start — ở khối lượng hiện tại hydration xong **trước** frame đầu; là bài toán **Architecture Efficiency**, lớn dần theo doanh nghiệp. Baseline 60 tháng (18.083 đơn): hydration = **4.5×** một lượt đọc ⇒ **đọc lặp chiếm ~80% chi phí**, tổng hợp ~20%. Mục tiêu: **một lượt đọc phục vụ nhiều Capability Context** — KHÔNG phá One Data Path, KHÔNG phá Capability Context, KHÔNG cache toàn cục, **UI không biết tới cache**. Năm hướng phải khảo sát (per-load scoped cache · shared aggregation pipeline · `summarise(rows)` contract · Drift query optimization · hybrid), chấm trên 8 trục. **CHƯA CHỌN HƯỚNG** — Founder Gate. Epic: WTM-167 | 📝 DRAFT | 2026-07-31 |
| [ADR-TON-020](ADR-TON-020-product-direction-local-first-file-bridge.md) | **Product Direction: Local-first → File Bridge → Validate** — chốt hướng C sau Product Reset 2026-08 (24 báo cáo). `Release → Local-first → File Bridge → validate với người dùng thật → Managed Platform CHỈ KHI có đủ bằng chứng`. **Không xây backend/OAuth trước nhu cầu thực tế**; D-4 + D-5 **giữ nguyên hiệu lực**. **File Bridge = capability chính thức**, KHÔNG phải giải pháp tạm — đọc file người dùng tự xuất từ sàn không phải tích hợp API và không cần backend; kể cả khi có Managed Platform, nó vẫn là đường duy nhất cho sàn không API · dữ liệu lịch sử · người không muốn giao khoá sàn. Ràng buộc khi xây: `Đọc → Chuẩn hoá → Đối chiếu → Xem trước → Xác nhận → Áp dụng` một transaction, **Provenance bắt buộc**, phải vào `.ttbk` v2, migration cộng thêm. **Product Reset không còn là backlog** — đề xuất còn lại chỉ hiện thực hoá qua Epic/ADR mới. Thứ tự delivery: WTM-175→176→177→178→179→180→167, rồi File Bridge | ✅ ACCEPTED (Founder) | 2026-08-01 |
| [ADR-TON-021](ADR-TON-021-business-journey-domain.md) | **Business Journey Domain Model** — Concept đòi 6 tầng (Intent → Plan → Milestone → Mission → Step → Task); sản phẩm có tầng 1 + thanh tiến độ ⇒ **không trả lời được câu hỏi định nghĩa của Journey**: *"tôi có đang đi đúng hướng không"*, vì không có kế hoạch để so. Quyết: **một cây `JourneyNode` đệ quy** (kind + parentId + orderIndex) thay vì 4 bảng cứng — thêm loại node mới = thêm giá trị enum, không phải migration; **`origin` (user/ruleTwin/ai) bắt buộc** để ranh giới ADR-TON-016 kiểm được chứ không chỉ là lời hứa (AI **không được** đặt `isCompleted`); **completion `derived`** gắn chỉ số thật, vì Alert/Forecast chỉ chạy được nếu tiến độ là dữ kiện đo được chứ không phải ô tick; **plan có version + reasonCodes**, lập lại kế hoạch KHÔNG ghi đè; **trạng thái 2 cấp** (5 giá trị Bible cho root, + `skipped`/`blocked` cho node vì Bible đòi *Failure Recovery*); **Template tách khỏi instance** (Playbook, Phase 2 chỉ sinh tại chỗ). Bắt buộc: migration cộng thêm · `.ttbk` **optional dataset** (thêm vào `all` sẽ làm mọi backup cũ không restore được — bài học WTM-177) · Rule Twin chạy không cần AI · **không** Tool Runtime · provider mới vào `kBusinessDataProviders`. 3 option (4 bảng cứng · cây node · JSON trong goal). **Sửa lại nhận định của chính bản nháp:** cây node **ÍT việc hơn** 4 bảng cứng (1 bảng/1 repo/1 mục `.ttbk`/1 migration thay vì 4) — chỗ đắt duy nhất là truy vấn đệ quy, tức một hàm chứ không phải bốn tầng hạ tầng ⇒ **không có đánh đổi nhanh-vs-mở-rộng ở đây**. Loại JSON vì không tham chiếu được Opportunity/Order nên mất `derived completion`. Migration cộng thêm, schema v8→v9, `BusinessGoal` giữ nguyên, `.ttbk` optional dataset. Kèm 4 dấu hiệu đo được từ dogfood để biết khi nào cần ADR thay thế **Amendment 1 (WTM-191):** cơ hội là **đầu vào** của hành trình — `sourceOpportunityId` (provenance một chiều, chiều ngược suy ra chứ không lưu), node mang `origin = user` (mô hình mô tả, con người quyết định) và là root `mission`, **`replan` cấm xoá node người bán tạo kể cả cây con** (rule sở hữu kế hoạch, không sở hữu quyết định), cột **không** FK (cơ hội là dữ liệu dẫn xuất — FK sẽ biến thứ tự ghi thành hợp đồng restore). Migration cộng thêm v10→v11. | ✅ ACCEPTED (Claude, Autonomous V2) | 2026-08-01 |
| [ADR-TON-021](ADR-TON-021-business-journey-domain.md) | **Business Journey Domain Model** — Concept đòi 6 tầng (Intent → Plan → Milestone → Mission → Step → Task); sản phẩm có tầng 1 + thanh tiến độ ⇒ **không trả lời được câu hỏi định nghĩa của Journey**: *"tôi có đang đi đúng hướng không"*, vì không có kế hoạch để so. Quyết: **một cây `JourneyNode` đệ quy** (kind + parentId + orderIndex) thay vì 4 bảng cứng — thêm loại node mới = thêm giá trị enum, không phải migration; **`origin` (user/ruleTwin/ai) bắt buộc** để ranh giới ADR-TON-016 kiểm được chứ không chỉ là lời hứa (AI **không được** đặt `isCompleted`); **completion `derived`** gắn chỉ số thật, vì Alert/Forecast chỉ chạy được nếu tiến độ là dữ kiện đo được chứ không phải ô tick; **plan có version + reasonCodes**, lập lại kế hoạch KHÔNG ghi đè; **trạng thái 2 cấp** (5 giá trị Bible cho root, + `skipped`/`blocked` cho node vì Bible đòi *Failure Recovery*); **Template tách khỏi instance** (Playbook, Phase 2 chỉ sinh tại chỗ). Bắt buộc: migration cộng thêm · `.ttbk` **optional dataset** (thêm vào `all` sẽ làm mọi backup cũ không restore được — bài học WTM-177) · Rule Twin chạy không cần AI · **không** Tool Runtime · provider mới vào `kBusinessDataProviders`. 3 option (4 bảng cứng · cây node · JSON trong goal). **Sửa lại nhận định của chính bản nháp:** cây node **ÍT việc hơn** 4 bảng cứng (1 bảng/1 repo/1 mục `.ttbk`/1 migration thay vì 4) — chỗ đắt duy nhất là truy vấn đệ quy, tức một hàm chứ không phải bốn tầng hạ tầng ⇒ **không có đánh đổi nhanh-vs-mở-rộng ở đây**. Loại JSON vì không tham chiếu được Opportunity/Order nên mất `derived completion`. Migration cộng thêm, schema v8→v9, `BusinessGoal` giữ nguyên, `.ttbk` optional dataset. Kèm 4 dấu hiệu đo được từ dogfood để biết khi nào cần ADR thay thế | ✅ ACCEPTED (Claude, Autonomous V2) | 2026-08-01 |
| [ADR-TON-024](ADR-TON-024-canonical-integration-platform-foundation.md) | **Canonical Integration Platform Foundation** — bốn luật để nối thêm mười lăm nền tảng mà **không phải đổi kiến trúc lần thứ hai**. (1) Tự động hoá dừng ở **liên kết danh tính**, **gộp bản ghi khách KHÔNG BAO GIỜ tự động** ở mọi mức tin cậy — hai người thật có thể dùng chung một số điện thoại, và *gộp nhầm hai khách tệ hơn không gộp*. (2) Settlement: `amount` luôn dương + `direction` tường minh (hoàn phí đảo chiều so với phí, cùng `kind`); `fundedBy` **bắt buộc, không mặc định** (voucher sàn tài trợ không phải chi phí người bán — nhầm chỗ này làm lợi nhuận sai theo **hướng tâng bốc**); **cấm tự phân bổ** khoản cấp-đơn xuống món. (3) Vendor Catalog + Capability Matrix là **dữ liệu**, không phải kiến thức AI: `recommended` là **hàm thuần không lưu**; ma trận có **ba** cột (`platformSupports`/`connectorCovers`/`verifiedOnDogfood`) và **AI chỉ được hứa ở cột ba**. (4) Canonical event: cùng một sự việc ⇒ cùng một mã, mã lạ ⇒ `unknown` không đoán. Không đụng dữ liệu hiện có, không đụng local-first | ✅ ACCEPTED (Founder) | 2026-08-07 |
| [ADR-TON-023](ADR-TON-023-business-input-and-product-type.md) | **Định vị mở rộng · Product Type · Producer = Business Input** — Dogfood Workizen (thử ghi chính Workizen vào Tổng Tài) làm lộ ra: `Product` bắt buộc `quantity`/`reorderLevel`, nên một sản phẩm số buộc người dùng **bịa một con số**, rồi app hành xử theo con số bịa đó — `quantity: 0` ⇒ Inventory kêu *"Hết hàng"* và Rule Engine **sinh cơ hội nhập hàng cho một phần mềm**. Founder: *"Đây không phải bug. Đây là phát hiện kiến trúc."* Quyết định: Tổng Tài là **AI Business OS cho SME** gồm Physical · Digital · Service · Hybrid; **Producer = Business Input** (Supplier chỉ là một loại); `ProductKind` canonical + `quantity`/`reorderLevel` **nullable** với `null` = *"không áp dụng"* chứ không phải 0 — cùng kỷ luật `costPrice` (WTM-204) và `paymentStatus` (WTM-211). Ba luật: không bịa dữ liệu · không ép doanh nghiệp số thành hàng hoá · không ép hàng hoá thành số. Hệ quả: schema **v14**, `stockStatus` trả `null` cho phi vật lý, `.ttbk` mang mã `kind`. Digital Cost Model tách riêng | ✅ ACCEPTED (Founder) | 2026-08-02 |
| [ADR-TON-022](ADR-TON-022-opportunity-scoring-with-missing-factors.md) | **Chấm điểm cơ hội khi thiếu dữ liệu** — `aiScore` là **hằng số theo luật** (85/70/65/75/60) nên *"sắp theo mức độ liên quan"* thực chất là *"sắp theo luật"*; `estimatedRoi` cũng hằng số, kéo theo **facet ROI** cho ra cùng thứ tự và **nhãn High Risk** chỉ nói lại luật nào bắn. Concept đòi 4 yếu tố (profit 40 · demand 30 · supplier 20 · competition 10) nhưng **2 trong 4 không tính được**: danh bạ nhà cung cấp là **dữ liệu mẫu**, và competition cần dữ liệu thị trường (D-5 không backend). 3 option (chuẩn hoá im lặng · `insufficient` cả điểm · **`insufficient` ở cấp yếu tố** ⭐). Chọn C: điểm chuẩn hoá trên phần có dữ liệu, đối tượng điểm **tự khai `coverage`** (0.7) + lý do từng yếu tố vắng; **`null` ≠ `0`** (0 nói *vô giá trị*, null nói *không ai biết*); độ phủ đi **vào prompt AI** để mô hình không giải thích 82/100 như phán đoán đầy đủ. Hệ quả: **gỡ** facet ROI (ẩn, giữ enum — quy tắc O-1 của Founder), **gỡ** nhãn High Risk, bước cuối action plan dùng tác động thật thay vì % ROI. ROI thật cần **giá vốn** mà `Product` không có. Điểm là dữ liệu dẫn xuất ⇒ **không** vào `.ttbk`, schema không đổi. Fixture test dùng `OpportunityScore.fixed` gắn **`@visibleForTesting`** — số viết tay trong `lib/` chính là khuyết tật vừa gỡ | ✅ ACCEPTED (Claude, Full Autonomous) | 2026-08-01 |

## Quyết định Founder khác (chưa thành ADR riêng)

| Quyết định | Nội dung | Nguồn |
|---|---|---|
| D-2 Package ID | `com.workizen.tongtai` (Android + iOS) | Founder GO khi split; tái xác nhận 2026-07-23 |
| D-3 Launch | Closed Beta → Open Beta → Production (tái dùng quy trình Hub) | Founder 2026-07-23 |
| D-4 Auth | Phase 2 không login (UUID local); Phase 3 Keycloak optional | Founder 2026-07-23 |
| D-5 Backend | Phase 2 không backend/không sync; Portal để sau | Founder 2026-07-23 |
| D-6 Monetization | Phase 2 free; Phase 3 RevenueCat (subscription/credits/premium AI) | Founder 2026-07-23 |
| D-8 Ngôn ngữ | VI primary, EN secondary | Founder 2026-07-23 |
| D-10 Export | Phase 2 có CSV export; Reports/BI Phase 3 | Founder 2026-07-23 |
| **D-11 Journey là trung tâm** | **Business Journey là trung tâm sản phẩm.** Finance · Consumer · Producer · Inventory · Reports **không** phải trung tâm — mọi capability mới phải **phục vụ Journey**. Khi có nhiều lựa chọn, ưu tiên thứ làm **Journey thông minh hơn** thay vì làm module riêng mạnh hơn. Áp vào Priority Matrix: điểm bằng nhau ⇒ việc phục vụ Journey xếp trước | Founder 2026-08-01 |
| D-12 Concept-1 phân nhóm | Công nợ = **view dẫn xuất** từ `paymentStatus` (làm được ngay) · CDP/CRM = **tiến hoá của Consumer hiện có**, không xây hub mới; Affiliate = Future · doanh thu theo kênh = **tự ghi trên đơn** (WTM-209), đồng bộ sàn = Phase 3 · Thuế / AI Product Discovery / Voice = **Future Capability** theo lộ trình | Founder 2026-08-01 |
| Model policy | Dev agent: Opus 4.8 mặc định; task khó/retry → Fable 5 | Founder 2026-07-16 |
| Evidence-Driven Runtime | Verdict từ evidence, không tin agent report; cấm placebo test | Founder 2026-07-14 |
| Self-planning | Runtime tự lập kế hoạch trong phạm vi feature branch; Founder giữ Vision/Direction/Goals/Architecture/main | Founder 2026-07-16 |

## Còn mở (chưa quyết)

Xem [OPEN-DECISIONS.md](OPEN-DECISIONS.md) (SSoT, cập nhật 2026-07-23) —
còn: mascot/icon (WTM-11) · scope WTM-83 · WTM-101 nghi trùng · SQLCipher.

## Tài liệu chuẩn khác (không phải ADR, nhưng ràng buộc như ADR)

| Tài liệu | Ràng buộc gì |
|---|---|
| [`../02-ARCHITECTURE/TONG_TAI_DESIGN_SYSTEM_V1.md`](../02-ARCHITECTURE/TONG_TAI_DESIGN_SYSTEM_V1.md) | Một ngôn ngữ thị giác cho toàn sản phẩm (Epic WTM-362). **CAM = bấm được · TÍM = AI đang hiểu · XÁM = chưa biết.** Ghi cả chỗ Design System **lệch khỏi spec Founder** vì WCAG, và lý do |
| [`../02-ARCHITECTURE/UI-IMPLEMENTATION-LEVELS.md`](../02-ARCHITECTURE/UI-IMPLEMENTATION-LEVELS.md) | Mức L0–L4 của từng màn (ADR-TON-015) |

## Quy tắc

Không silently mâu thuẫn ADR. Thay đổi = ADR mới `ADR-TON-00N-*.md` ghi rõ
supersede, chờ Founder duyệt, cập nhật index này.
